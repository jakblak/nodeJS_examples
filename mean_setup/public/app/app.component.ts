import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'mt-app',
  templateUrl: 'app.component.html'
})

export class AppComponent {

  title: string = "My MEAN Setup";
}